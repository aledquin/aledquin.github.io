report_path -show_path_index -max -max_paths 2000  > max_timing.rpt
report_path -show_path_index -max -max_paths 2000 -path_type full_clock_expanded -nets -trans -cap -rail_voltage -final_voltage -crosstalk_delta -wire_delay >> max_timing.rpt
report_path -show_path_index -min -max_paths 2000  > min_timing.rpt
report_path -show_path_index -min -max_paths 2000 -path_type full_clock_expanded -nets -trans -cap -rail_voltage -final_voltage -crosstalk_delta -wire_delay >> min_timing.rpt

report_pbsa_calculation [get_timing_paths -max -max_paths 20] > pbsa.rpt

report_analysis_coverage -status_details untested -sort_by check_type > coverage.rpt

report_clock > clock.rpt
report_clock_arrivals > clock_arrivals.rpt

report_crosstalk_delay_sources -max_aggressors 3  -max_nets 100 -max -aggressor_contributions > si_delay_max.rpt
report_crosstalk_delay_sources -max_aggressors 3  -max_nets 100 -min -aggressor_contributions > si_delay_min.rpt

update_noise
report_noise -nworst 200 > noise.rpt

report_constraint -max_capacitance -verbose > maxcap.rpt
report_constraint -max_transition -verbose > maxtrans.rpt
report_constraint -min_pulse_width -verbose > minpulse.rpt

